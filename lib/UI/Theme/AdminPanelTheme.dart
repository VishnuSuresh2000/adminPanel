import 'package:flutter/material.dart';

ThemeData adminPanelTheme = ThemeData(
  primaryColor: Colors.white,
  appBarTheme: AppBarTheme(
    centerTitle: true,
    textTheme: TextTheme(
      headline6: TextStyle(
        color: Colors.black54,
        fontSize: 20
      )
    )
  )
);
